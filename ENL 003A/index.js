/* ==========================================================================
   JavaScript Controller: Advanced Dynamic Features & Interactive Widgets
   ========================================================================== */

// 1. Pre-embedded content fallbacks for local file protocol (CORS issues) or offline use
const EMBEDDED_DOCS = {
  poetry: `# Where Theology and Non-Dualism Dance: Analyzing the Immanent Divine in Hopkins’s "God's Grandeur"

**By:** Stephen Ta  
**Class:** ENL 003A  
**Instructor:** Dr. Iris Jamahl Dunkle  
**Date:** April 13, 2026

---

The tension between human action and divine force has long been widely discussed through religions, art, and literature. Gerard Manley Hopkins’s "God’s Grandeur," for me, is one of the best examples when it comes to this topic. Structured as a Petrarchan sonnet, the poem uses dense sonic textures to highlight a paradox: despite humanity’s attempts to detach itself from the natural world, divine energy continues to sustain it. As someone who has been surrounded by Eastern philosophy and ideology all my life, this poem linked my thinking to Daoism and Prana. It made me realize that divinity is not a distant entity that we can’t reach, but an immanent presence in our world that is only obscured by the ignorance of humans.

In the first eight lines (the octave), Hopkins establishes God’s presence in our world, then turns to a critique of how humans have separated themselves from that world. Hopkins opens with an absolute declaration:

> "The world is charged with the grandeur of God." (Hopkins 1)

The word "charged" is used as a metaphor of electrical energy, suggesting that the universe acts as a battery full of divine power. In Eastern philosophy, this "charge" is like Qi or Prana—the vital life force that animates the cosmos. This energy is also illustrated through two contrasting similes. First, it:

> "will flame out, like shining from shook foil" (Hopkins 2)

This describes the sudden flash of divine revelation that occurs when the world is examined closely. Second:

> "It gathers to a greatness, like the ooze of oil / Crushed." (Hopkins 3-4)

This implies that the image changes from something sudden to something slow, showing that God’s essence can appear both quickly and gradually, and that it is deep, powerful, and persistent.

Besides this declaration, the poem’s tone changes to critique human blindness. Hopkins asks:

> "Why do men then now not reckon his rod?" (Hopkins 4)

This rhetorical question points out the concept of spiritual ignorance. In Advaita Vedanta, this condition is known as *Maya*—the illusion that the material, industrial world is the ultimate reality. Hopkins emphasizes the repetitive trap of this illusion through repetition and internal rhyme:

> "Generations have trod, have trod, have trod" (Hopkins 5)

The heavy rhythm mirrors the mechanical march of industrialization and the repetitive hardship of human labor.

The impact of this industrial mindset is powerful:

> "And all is seared with trade; bleared, smeared with toil" (Hopkins 6)

Through the heavy consonance and the assonance of the "eared" sound, Hopkins creates an auditory representation of a world being tired and exhausted. The natural surface of the earth now:

> "wears man’s smudge and shares man’s smell: the soil" (Hopkins 7)

This feeling of separation reaches its peak in the physical distance between humans and nature:

> "the soil / Is bare now, nor can foot feel, being shod" (Hopkins 7-8)

The shoe becomes a metaphor for the artificial, physical barriers humanity builds, cutting off our direct connection to the Earth and to the divine reality beneath it.

Despite the bleakness of the first eight lines, the poem changes dramatically at the volta (line 9), marking the beginning of the sestet. Hopkins declares:

> "And for all this, nature is never spent" (Hopkins 9)

This line asserts the indestructible nature of the divine presence. In Daoism, the *Dao* is often described as a well that is used but never used up—an inexhaustible source that generates and sustains all forms. Hopkins echoes this exact philosophy: beneath the "bleared" surface of the human world lies a wellspring of constant renewal.

He locates this renewal in the depths of existence:

> "There lives the dearest freshness deep down things" (Hopkins 10)

The use of alliteration in "dearest," "deep," and "down" draws the reader’s focus inward and downward, moving away from the superficial "smudge" of the surface into the ontological core of reality. This "freshness" is the ultimate reality, the pure block of Eastern thought, entirely untouched by human corruption.

Hopkins then further explains this cycle through the image of the transition from day to night:

> "And though the last lights off the black West went / Oh, morning, at the brown brink eastward, springs—" (Hopkins 11-12)

Hopkins uses enjambment as the line spills eastward, mimicking the inevitable rise of the sun. Additionally, the setting of the sun in the "black West" symbolizes spiritual darkness and human decline, but the rising in the East represents an eternal, cosmic rebirth powered by the divine.

The poem concludes by revealing the source of this continuous renewal:

> "Because the Holy Ghost over the bent / World broods with warm breast and with ah! bright wings." (Hopkins 13-14)

In these final lines, Hopkins utilizes his signature "sprung rhythm" to break the confines of traditional meter, using the exclamation "ah!" to force a pause of pure wonder and awe. The image of the Holy Ghost as a mother bird brooding over a nest transforms the distorted, bent world into a child under the warm care of divinity.

When reading this final metaphor, my thoughts became a common playground where Western theology and Eastern non-dualism dance and play together. The divine is not situated in a distant heaven, but is intimately wrapped around the fragile world, sustaining it with a protective, maternal energy. The "bright wings" offer a final contrast to the "black West" and the "smudge" of human toil, returning to the flashing, electrical grandeur of the opening line. Ultimately, Hopkins states that through poetic craft, while human actions may obscure the surface of the world, the underlying reality remains ever-present, protective, and eternally vibrant.

---

### Work Cited

Hopkins, Gerard Manley. "God's Grandeur." *Poetry Foundation*, www.poetryfoundation.org/poems/44395/gods-grandeur.`,
  
  lottery: `# How Tradition Can Become Tragedy: Analysis of Shirley Jackson's "The Lottery"

**By:** Stephen Ta  
**Class:** ENL 003A  
**Instructor:** Professor Iris Dunkle  
**Date:** 11th May 2026

---

Every June 27th, the villagers gather in the square: children laugh, neighbors chat, and the morning sun shines on an ordinary American town, until someone is beaten to death with rocks. Shirley Jackson's "The Lottery" opens with a lie: the lie of normalcy. Beneath its cheerful surface, the story conceals one of literature's most devastating revelations about human nature: that ordinary people—good neighbors and loving parents—are fully capable of ritualized murder simply because tradition tells them it is the right thing to do. Through control of setting, plot structure, and literary technique, Jackson constructs a trap for the reader that mirrors the trap society sets for its own members. Her central argument is simple: blind obedience to tradition does not require monsters, because ordinary people are already sufficient.

To understand how Jackson builds this trap, it helps to start with the title and setting. "The Lottery" is a word that usually carries a promise of reward, yet Jackson exploits that promise ruthlessly, as readers expect a winner, not a corpse. This misdirection produces what can be called an epiphany—a sudden and total rewriting of everything the reader thought they understood. When the stones are finally thrown, the whole village is illuminated in a terrifying new light. 

The setting also deepens this effect considerably. In particular, the story takes place on a bright June morning in what is clearly a mid-twentieth-century American town, where men discuss "planting and rain, tractors and taxes" (Jackson). Yet the ritual unfolding in the square belongs to something far older and darker, closer to a medieval sacrifice than a modern event. Jackson places these two worlds side by side on purpose. The tractors and the stoning exist in the same afternoon, in the same sunlight, among the same smiling neighbors. Her point is uncomfortable: modernity is only a surface. Beneath it, primitive violence has never left.

The plot structure of the story is also engineered with similar precision to create and sustain tension. In the exposition, Jackson introduces the village with a calm and pleasant hand. Children run freely, neighbors chat, and the day feels like a holiday. But foreshadowing quietly poisons the warmth. Bobby Martin stuffs his pockets with stones, and the boys construct "a great pile of stones in one corner" (Jackson). On the surface, this represents children playing. Looking back after finishing the story, it becomes clear this was the preparation of weapons. Jackson trusts the reader to feel the unease before they can name it.

The rising action begins with Mr. Summers' arrival with the black box and the lottery drawing. Jackson narrates all of this in a third-person point of view, reporting only what characters do and say, never what they think or feel. This restraint is one of the story's most important stylistic choices. Because the reader cannot enter any character's inner life, the villagers appear cheerful, cooperative, and utterly normal, which is precisely what makes them so frightening. The prose style reinforces this effect. Jackson's diction is plain, and her pace is slow, luring the reader into the trap she has prepared.

Conflict arises when Bill Hutchinson draws the marked slip, and his wife Tessie immediately begins to protest. Tessie functions as the story's protagonist, the figure through whom the reader feels the horror of the ritual. Yet, the true antagonist is not a single villain. It is the tradition itself—something the village has built around an act of murder and called "culture." Tessie cries out that Bill was not given enough time to choose his paper fairly, and the men in charge dismiss her completely. As critic Joan Wylie Hall argues, this moment exposes the deeply patriarchal structure of the village, where male authority goes unchallenged and a woman's objection, no matter how desperate, carries no weight (Hall). Tessie is not simply outvoted; she is not heard at all. With that, Jackson makes clear that the lottery's violence is not only physical but also built into the social order that surrounds it.

Tessie’s protest is more revealing because she only objects when she becomes the victim. She is not challenging the lottery on moral grounds; she is complaining that the draw was conducted unfairly against her family. She accepted the tradition completely until it turned against her. This is one of Jackson's sharpest observations: people rarely question a system until they become its victim, and even then, they tend to argue about procedure rather than purpose.

The falling action moves with a surprising speed. The Hutchinson family draws again, the papers are opened, and the resolution arrives without ceremony. Tessie unfolds the slip with the black spot, and the village surrounds her. Her final words, *"It isn't fair, it isn't right"* (Jackson), are the most important in the story, not because they change anything, but because they change nothing. The community, including her own children, picks up the stones and carries out the sentence. The conflict is resolved the way it always has been, with total obedience and no questioning.

Throughout all of this, Jackson also uses various literary devices to reinforce her argument about tradition. The black wooden box is the story's central symbol. Rotting, splintered, and barely holding together, it is described as having the "original wood color" showing through where the paint has worn away (Jackson). The village refuses to replace it, not out of reverence for the box itself, but out of a fear of changing anything related to the ritual. The box is a symbol of the tradition it contains: deteriorating, purposeless, and stubborn. This connects directly to the story's theme: communities will defend broken traditions simply because abandoning them feels more dangerous than continuing them.

Jackson also employs a brief flashback to explain the box's origins, tracing it back to "the first people" who settled the village (Jackson). This gesture reaches backward to give the tradition a fake legitimacy. Paired with this is something closer to a flash forward, delivered through Old Man Warner, who warns that abandoning the lottery will send civilization back to "living in caves" and eating "stewed chickweed and acorns" (Jackson). His logic is the logic of every society that has ever committed an atrocity in the name of order: that stopping the ritual will break everything apart. Jackson frames this belief with quiet irony. Warner is the village's most passionate defender of a tradition, yet he cannot explain it and has never examined it.

On the level of language, Jackson uses figurative language effectively. When Tessie arrives late to the square and makes a joke, "soft laughter ran through the crowd" (Jackson). The laughter is described as a physical thing moving through bodies, bringing the group together. It is a small detail, but it does important work. The crowd is not a collection of individuals in this moment; it is a single organism, and its gentleness is as disturbing as its violence because both come from the same source.

In conclusion, "The Lottery" earns its reputation as one of American literature's most enduring and unsettling stories because every element of its craft serves a coherent argument. The plot structure builds a perfect trap. The setting creates a collision between the comfortable and the brutal. The symbolism, the point of view, the restrained prose style—all of them make the reader feel, rather than simply understand, how ordinary people sustain cruelty. Tessie Hutchinson does not die because her neighbors are evil. She dies because no one, including Tessie herself, thought to ask why the lottery existed in the first place. That is the lesson Jackson leaves behind, quiet but worth thinking about: traditions may not be the truth, and questioning them may be the wise thing to do.

---

### Works Cited

* Hall, Joan Wylie. "The Short Fiction." *Shirley Jackson: A Study of the Short Fiction*, Twayne Publishers, 1993. *Gale Literature: Twayne's Author Series*, link.gale.com/apps/doc/CX1715000013/G-Twayne.
* Jackson, Shirley. "The Lottery." *The New Yorker*, 26 June 1948.`,
  
  art: `# The Texture of a Season: Abstraction and Emotion in Roland Petersen’s Spring Luncheon

**By:** Stephen Ta  
**Class:** ENL 003A  
**Instructor:** Dr. Iris Jamahl Dunkle  
**Date:** April 13, 2026

---

When I first saw Roland Petersen’s *Spring Luncheon*, I was struck by a sensation that is both striking and puzzling: it is abstract, yet still distinguishable. This tension between abstraction and legibility is at the heart of what makes the painting so compelling—a tension that Petersen crafted with deliberate precision. Through formal choices in color, contrast, and the selective omission of detail, Petersen does not simply depict a scene; he captures a visceral feeling.

![Roland Petersen's Spring Luncheon (1961)](spring_luncheon.png)

Perhaps the most remarkable aspect of *Spring Luncheon* lies in its color palette. The chosen colors are the epitome of spring: soft pinks, warm yellows, fresh greens, and open sky blues sweep across the canvas in bold strokes. However, what elevates this palette beyond a simple, pleasant representation is the integration of darker hues. Thick slashes of deep indigo, dark teal, and heavy crimson cut across the center of the canvas, carving a stark boundary between the picnic and the horizon. 

Rather than disrupting the sunlit mood, these dark, weighty tones provide structural contrast and shape. They ground the composition and give the lighter yellow and soft pink strokes a vivid backdrop against which to truly breathe and expand. This combination of light and dark is far from accidental; Petersen demonstrates an unmatched sense of color value. By understanding how colors relate to one another in terms of lightness and darkness, he uses those relationships to create depth, movement, and emotional resonance. The result is a canvas that feels actively alive.

Looking beyond the color, the composition reveals another layer of meaning. Heavy vertical bands representing "trees" slice straight down the left side of the canvas, while thick, rectangular "sheets" of vibrant color stretch horizontally across the middle ground. These stark geometric planes frame the composition, drawing the eye down to the cluster of round "plates of food" gathered on the bright white tablecloth in the lower-right foreground. 

None of these objects is painted with precision or photographic accuracy. Instead, Petersen uses a heavy *impasto* technique, layering the oil paint so thickly that the physical texture of the canvas mirrors the raw energy of the season. Because of this dense, layered brushwork, the objects do not require clean outlines. Instead, they emerge from the ridges of the paint itself, existing as tactile suggestions and impressions. 

This "lack" of detail is not a flaw, but rather one of the painting’s most successful decisions. By stripping away the specificity that would attach the scene to a particular time, place, or person, Petersen gives the painting a universal quality. The picnic in *Spring Luncheon* does not belong to any single family or afternoon; it belongs to anyone who has ever felt the warmth of a spring day.

This universality is the painting’s greatest achievement, making the viewer's emotional response genuine. *Spring Luncheon* is not a depiction of a specific spring; it is spring as an ambient feeling. Standing before it, one is filled with warmth, almost feeling the sun shining down. The painting does not ask the viewer to remember a particular memory or recognize a specific place; it asks only that they open themselves to a season, and to the easy joy and unhurried light that the word "spring" evokes in the mind. Petersen achieves this not through detail but through its absence, relying on color and form working together in perfect harmony.

Another compelling aspect of *Spring Luncheon* is how it rewards continued attention. It is one of those paintings where the longer one gazes at it, the more resonant it becomes. With each return of the eye, new relationships between colors become visible, new shapes emerge from the layered brushwork, and a deeper feeling of warmth and light makes itself known. Petersen’s painting does not reveal itself all at once. It unfolds gradually, like the season it portrays. In this way, the viewer does not merely observe the scene; they actively participate in it. *Spring Luncheon* is not simply a painting about spring—it is, in its very structure and experience, an act of spring.

---

### Work Cited

Petersen, Roland. *Spring Luncheon*. 1961, Jan Shrem and Maria Manetti Shrem Museum of Art, Davis.`,
  
  journal: `# Reflective Journal: The Texture of Inquiry

**By:** Stephen Ta  
**Class:** ENL 003A  
**Instructor:** Dr. Iris Jamahl Dunkle  
**Date:** June 9, 2026

---

Over the course of this semester in ENL 003A, my development as a writer has been defined by a transition from static, linear analysis to dynamic, multi-modal synthesis. Moving between diverse subjects—from the strict theological structure of Gerard Manley Hopkins’s poetry, to the social mechanics of Shirley Jackson’s fiction, and finally to the tactile, visual abstraction of Roland Petersen’s painting—has challenged me to adapt my rhetorical strategies to the medium at hand.

Reading Hopkins’s *God’s Grandeur* pushed me to bridge disparate intellectual frameworks. By juxtaposing Western Christian theology with Eastern Daoist and Vedantic concepts of Qi, Prana, and Maya, I learned that writing can serve as a "common playground" where conflicting ideologies can dialogue. This cross-disciplinary approach deepened my understanding of how poetic form—specifically Hopkins's enjambment and "sprung rhythm"—works physically to convey spiritual energy. In analyzing *The Lottery*, I had to shift my focus to the integration of external scholarly perspectives. Incorporating critic Joan Wylie Hall taught me how to weave secondary sources seamlessly into my own arguments, transforming a simple plot summary into a sharp critique of patriarchal traditions. Finally, writing about Roland Petersen’s *Spring Luncheon* was a breakthrough in translating visual sensation into text. It forced me to move beyond descriptive language to analyze how abstract color values and impasto textures evoke universal, ambient emotions.

Through these assignments, I have learned that strong writing is not just about conveying information, but about designing an experience for the reader. My future writing goals are to continue refining this synthesis. I want to build on this cross-disciplinary foundation by pursuing multi-modal writing that combines textual analysis with digital and visual media. Furthermore, I aim to focus on structural editing in my future projects, ensuring that my essays mirror the deep, underlying textures of the subjects I explore.`
};

const DOC_SEQUENCE = ['poetry', 'lottery', 'art', 'journal'];
const DOC_TITLES = {
  poetry: "Where Theology & Non-Dualism Dance",
  lottery: "Tradition & Terror in Jackson's Fiction",
  art: "The Texture of Petersen's Spring Luncheon",
  journal: "The Semester Reflective Journal"
};

let currentDocId = null;
let journalUnlocked = false;

// Side column highlights cache elements
let cachedPoemLines = [];
let cachedPoemParagraphs = [];
let cachedArtMaskPaths = [];
let cachedArtParagraphs = [];
let cachedArtPaintBox = null;

// 2. Initialization
document.addEventListener('DOMContentLoaded', () => {
  detectFileProtocol();
  initializePrismEffects();
  initializeNavigation();
  initializePaintEngine();
  initializeSynthesisPortal();
  initializeAudioEngine();
  initializeLotteryRitual();
  
  // Clean scroll highlights handler (deterministic closest-to-center algorithm)
  window.addEventListener('scroll', handleScrollHighlights);
});

// Detect local file protocol
function detectFileProtocol() {
  const isLocalFile = window.location.protocol === 'file:';
  const banner = document.getElementById('local-server-banner');
  const closeBtn = document.getElementById('close-banner');
  if (isLocalFile && banner) {
    setTimeout(() => banner.classList.add('active'), 1000);
    closeBtn.addEventListener('click', () => banner.classList.remove('active'));
  }
}

// 3. Prism Cover Screen Mechanics (Fixing bottom node hover loop glitch)
let hoveredNodeId = null;

function initializePrismEffects() {
  const prism = document.getElementById('interactive-prism');
  const cover = document.getElementById('cover-view');
  if (!prism) return;
  
  // Track dynamic mouse tilt when NO navigation node is hovered
  document.addEventListener('mousemove', (e) => {
    if (!cover.classList.contains('active')) return;
    
    const rect = prism.getBoundingClientRect();
    const pCenterX = rect.left + rect.width / 2;
    const pCenterY = rect.top + rect.height / 2;
    
    let targetRotX = 0;
    let targetRotY = 0;
    
    if (hoveredNodeId) {
      // Tilt prism deliberately towards the hovered navigation node
      switch(hoveredNodeId) {
        case 'poetry': targetRotX = 12; targetRotY = -12; break; // top left
        case 'lottery': targetRotX = 12; targetRotY = 12; break; // top right
        case 'art': targetRotX = -12; targetRotY = -12; break;   // bottom left
        case 'journal': targetRotX = -12; targetRotY = 12; break; // bottom right
      }
    } else {
      // Mouse dynamic tracking
      targetRotX = (pCenterY - e.clientY) / 12;
      targetRotY = (e.clientX - pCenterX) / 12;
    }
    
    prism.style.transform = `rotateX(${Math.max(-18, Math.min(18, targetRotX))}deg) rotateY(${Math.max(-18, Math.min(18, targetRotY))}deg)`;
  });

  prism.addEventListener('mouseleave', () => {
    if (!hoveredNodeId) {
      prism.style.transform = 'rotateX(0deg) rotateY(0deg)';
    }
  });
  
  const nodes = document.querySelectorAll('.nav-node');
  nodes.forEach(node => {
    const docId = node.getAttribute('data-doc');
    const beamId = getBeamIdForDoc(docId);
    const beam = document.getElementById(beamId);
    
    node.addEventListener('mouseenter', () => {
      hoveredNodeId = docId; // active override
      if (beam) beam.classList.add('active');
      document.body.setAttribute('data-theme', docId);
    });
    
    node.addEventListener('mouseleave', () => {
      hoveredNodeId = null;
      if (beam) beam.classList.remove('active');
      if (cover.classList.contains('active')) {
        document.body.setAttribute('data-theme', 'default');
      }
    });
  });
}

function getBeamIdForDoc(docId) {
  switch(docId) {
    case 'poetry': return 'beam-top-left';
    case 'lottery': return 'beam-top-right';
    case 'art': return 'beam-bottom';
    case 'journal': return 'beam-center';
    default: return '';
  }
}

// 4. View Switching & Content Nav Router
function initializeNavigation() {
  const cover = document.getElementById('cover-view');
  const content = document.getElementById('content-view');
  const nodes = document.querySelectorAll('.nav-node');
  const tabLinks = document.querySelectorAll('.tab-link');
  const btnBack = document.getElementById('btn-back-home');
  const btnPrev = document.getElementById('btn-prev-doc');
  const btnNext = document.getElementById('btn-next-doc');
  
  nodes.forEach(node => {
    node.addEventListener('click', () => navigateToDoc(node.getAttribute('data-doc')));
  });

  tabLinks.forEach(tab => {
    tab.addEventListener('click', () => navigateToDoc(tab.getAttribute('data-doc')));
  });

  if (btnBack) {
    btnBack.addEventListener('click', () => {
      content.classList.remove('active');
      cover.classList.add('active');
      document.body.setAttribute('data-theme', 'default');
      window.scrollTo({ top: 0, behavior: 'smooth' });
      currentDocId = null;
      updateSoundscapeParameters('default');
    });
  }

  if (btnPrev) {
    btnPrev.addEventListener('click', () => {
      const idx = DOC_SEQUENCE.indexOf(currentDocId);
      if (idx > 0) navigateToDoc(DOC_SEQUENCE[idx - 1]);
    });
  }
  
  if (btnNext) {
    btnNext.addEventListener('click', () => {
      const idx = DOC_SEQUENCE.indexOf(currentDocId);
      if (idx < DOC_SEQUENCE.length - 1) navigateToDoc(DOC_SEQUENCE[idx + 1]);
    });
  }

  window.addEventListener('scroll', () => {
    if (content.classList.contains('active')) {
      const scrollTop = window.scrollY || document.documentElement.scrollTop;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const progress = (scrollTop / Math.max(1, docHeight)) * 100;
      const bar = document.getElementById('scroll-progress');
      if (bar) bar.style.width = `${progress}%`;
    }
  });
}

function navigateToDoc(docId) {
  const cover = document.getElementById('cover-view');
  const content = document.getElementById('content-view');
  const tabs = document.querySelectorAll('.tab-link');
  const lockScreen = document.getElementById('journal-lock-container');
  const viewer = document.getElementById('document-viewer');
  
  currentDocId = docId;
  document.body.setAttribute('data-theme', docId);
  updateSoundscapeParameters(docId);
  
  cover.classList.remove('active');
  content.classList.add('active');
  
  tabs.forEach(tab => {
    if (tab.getAttribute('data-doc') === docId) {
      tab.classList.add('active');
    } else {
      tab.classList.remove('active');
    }
  });
  
  // Handle lock status for Reflective Journal
  if (docId === 'journal' && !journalUnlocked) {
    lockScreen.classList.add('active');
    viewer.style.display = 'none';
    setTimeout(() => resetSynthesisMixer(), 50);
  } else {
    lockScreen.classList.remove('active');
    viewer.style.display = 'block';
    loadDocumentText(docId);
  }
  
  updateFooterNavigation(docId);
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

// Fetch and render markdown text
async function loadDocumentText(docId) {
  const viewer = document.getElementById('document-viewer');
  if (!viewer) return;
  
  viewer.innerHTML = `<div class="loading-spinner"><i class="fa-solid fa-circle-notch fa-spin"></i> Loading text...</div>`;
  let md = '';
  
  try {
    const res = await fetch(`${docId}.md`);
    if (!res.ok) throw new Error();
    md = await res.text();
  } catch (err) {
    md = EMBEDDED_DOCS[docId] || `# Not Found`;
  }
  
  viewer.innerHTML = parseMarkdown(md);
  
  // Pre-process and cache elements for smooth scroll highlighting
  if (docId === 'art') {
    preprocessArtParagraphs();
  } else if (docId === 'poetry') {
    preprocessPoetryParagraphs();
  } else if (docId === 'lottery') {
    resetLotteryRitualGame();
  }
}

// 5. Glitch-Free Scroll Highlights Engine (Closest-to-center algorithm)
function preprocessArtParagraphs() {
  const viewer = document.getElementById('document-viewer');
  const paragraphs = viewer.querySelectorAll('p');
  
  paragraphs.forEach(p => {
    const text = p.textContent.toLowerCase();
    if (text.includes("vertical bands representing") || text.includes("trees")) {
      p.setAttribute('data-target-mask', 'mask-trees');
    } else if (text.includes("indigo") || text.includes("dark teal") || text.includes("crimson")) {
      p.setAttribute('data-target-mask', 'mask-slashes');
    } else if (text.includes("tablecloth") || text.includes("horizon")) {
      p.setAttribute('data-target-mask', 'mask-tablecloth');
    } else if (text.includes("plates of food") || text.includes("plates")) {
      p.setAttribute('data-target-mask', 'mask-plates');
    }
  });

  cachedArtParagraphs = Array.from(viewer.querySelectorAll('p[data-target-mask]'));
  cachedArtMaskPaths = Array.from(document.querySelectorAll('.painting-mask-path'));
  cachedArtPaintBox = document.querySelector('.interactive-painting-box');
}

function preprocessPoetryParagraphs() {
  const viewer = document.getElementById('document-viewer');
  const paragraphs = viewer.querySelectorAll('p');
  
  paragraphs.forEach(p => {
    const text = p.textContent.toLowerCase();
    if (text.includes("first eight lines") || text.includes("octave")) {
      p.setAttribute('data-target-lines', '1,2,3,4,5,6,7,8');
    } else if (text.includes("charged") || text.includes("hopkins 1")) {
      p.setAttribute('data-target-lines', '1');
    } else if (text.includes("foil") || text.includes("hopkins 2")) {
      p.setAttribute('data-target-lines', '2');
    } else if (text.includes("ooze") || text.includes("hopkins 3-4")) {
      p.setAttribute('data-target-lines', '3,4');
    } else if (text.includes("rod") || text.includes("hopkins 4")) {
      p.setAttribute('data-target-lines', '4');
    } else if (text.includes("generations") || text.includes("hopkins 5")) {
      p.setAttribute('data-target-lines', '5');
    } else if (text.includes("seared") || text.includes("toil") || text.includes("hopkins 6")) {
      p.setAttribute('data-target-lines', '6');
    } else if (text.includes("smudge") || text.includes("smell") || text.includes("hopkins 7")) {
      p.setAttribute('data-target-lines', '7');
    } else if (text.includes("soil") || text.includes("shod") || text.includes("hopkins 7-8")) {
      p.setAttribute('data-target-lines', '7,8');
    } else if (text.includes("volta") || text.includes("spent") || text.includes("hopkins 9")) {
      p.setAttribute('data-target-lines', '9');
    } else if (text.includes("dearest freshness") || text.includes("hopkins 10")) {
      p.setAttribute('data-target-lines', '10');
    } else if (text.includes("last lights") || text.includes("morning") || text.includes("hopkins 11-12")) {
      p.setAttribute('data-target-lines', '11,12');
    } else if (text.includes("holy ghost") || text.includes("wings") || text.includes("hopkins 13-14")) {
      p.setAttribute('data-target-lines', '13,14');
    }
  });

  cachedPoemParagraphs = Array.from(viewer.querySelectorAll('p[data-target-lines]'));
  cachedPoemLines = Array.from(document.querySelectorAll('.poem-line'));
}

function handleScrollHighlights() {
  const theme = document.body.getAttribute('data-theme');
  const centerY = window.innerHeight * 0.45;
  
  if (theme === 'poetry') {
    if (cachedPoemParagraphs.length === 0) return;
    
    let closestP = null;
    let minDistance = Infinity;
    
    cachedPoemParagraphs.forEach(p => {
      const rect = p.getBoundingClientRect();
      const pCenterY = rect.top + rect.height / 2;
      const dist = Math.abs(pCenterY - centerY);
      
      if (dist < minDistance) {
        minDistance = dist;
        closestP = p;
      }
    });
    
    const allViewerParagraphs = document.querySelectorAll('#document-viewer p');
    allViewerParagraphs.forEach(p => p.classList.remove('reading-focused'));
    
    if (closestP && minDistance < 220) {
      closestP.classList.add('reading-focused');
      const linesAttr = closestP.getAttribute('data-target-lines');
      const lineIndices = linesAttr.split(',').map(n => parseInt(n));
      
      cachedPoemLines.forEach(line => {
        const lNum = parseInt(line.getAttribute('data-line'));
        if (lineIndices.includes(lNum)) {
          line.classList.add('active');
        } else {
          line.classList.remove('active');
        }
      });
    } else {
      cachedPoemLines.forEach(line => line.classList.remove('active'));
    }
    
  } else if (theme === 'art') {
    if (cachedArtParagraphs.length === 0) return;
    
    let closestP = null;
    let minDistance = Infinity;
    
    cachedArtParagraphs.forEach(p => {
      const rect = p.getBoundingClientRect();
      const pCenterY = rect.top + rect.height / 2;
      const dist = Math.abs(pCenterY - centerY);
      
      if (dist < minDistance) {
        minDistance = dist;
        closestP = p;
      }
    });
    
    const allViewerParagraphs = document.querySelectorAll('#document-viewer p');
    allViewerParagraphs.forEach(p => p.classList.remove('reading-focused'));
    cachedArtMaskPaths.forEach(path => path.classList.remove('active'));
    
    if (closestP && minDistance < 220) {
      closestP.classList.add('reading-focused');
      const targetMask = closestP.getAttribute('data-target-mask');
      const activePath = document.getElementById(targetMask);
      
      if (activePath) activePath.classList.add('active');
      if (cachedArtPaintBox) cachedArtPaintBox.classList.add('highlighting');
    } else {
      if (cachedArtPaintBox) cachedArtPaintBox.classList.remove('highlighting');
    }
  }
}

// 6. Dynamic Background Paint Engine (Canvas)
function initializePaintEngine() {
  const canvas = document.getElementById('paint-canvas');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  
  function resize() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  }
  
  resize();
  window.addEventListener('resize', resize);
  
  let brushColorIndex = 0;
  const colors = [
    'rgba(244, 143, 177, 0.22)',
    'rgba(255, 235, 116, 0.22)',
    'rgba(129, 230, 217, 0.22)',
    'rgba(99, 179, 237, 0.22)'
  ];

  window.addEventListener('mousemove', (e) => {
    if (document.body.getAttribute('data-theme') !== 'art') return;
    
    ctx.beginPath();
    ctx.arc(e.clientX, e.clientY, 35, 0, Math.PI * 2);
    ctx.fillStyle = colors[brushColorIndex];
    ctx.fill();
    
    if (Math.random() < 0.05) {
      brushColorIndex = (brushColorIndex + 1) % colors.length;
    }
  });
  
  function step() {
    if (document.body.getAttribute('data-theme') === 'art') {
      ctx.fillStyle = 'rgba(4, 8, 16, 0.015)';
      ctx.fillRect(0, 0, canvas.width, canvas.height);
    }
    requestAnimationFrame(step);
  }
  step();
}

// 7. Drag-and-Drop Color Synthesis Portal (Lag-Free Caching & GPU Transforms)
function initializeSynthesisPortal() {
  const board = document.getElementById('mixer-board');
  const orbs = document.querySelectorAll('.drag-orb');
  const target = document.getElementById('mixer-dropzone');
  if (!board) return;
  
  let activeOrb = null;
  let offsetX = 0, offsetY = 0;
  
  // Cache variables for drag performance to prevent layout thrashing
  let boardLeft = 0;
  let boardTop = 0;
  let boardWidth = 0;
  let boardHeight = 0;
  let targetX = 0;
  let targetY = 0;
  
  let orbCoords = {
    poetry: { x: 40, y: 30, snapped: false },
    lottery: { x: 505, y: 30, snapped: false },
    art: { x: 272, y: 225, snapped: false }
  };
  
  orbs.forEach(orb => {
    orb.addEventListener('mousedown', startDrag);
    orb.addEventListener('touchstart', startDrag, { passive: false });
  });
  
  function startDrag(e) {
    activeOrb = this;
    const type = activeOrb.getAttribute('data-type');
    if (orbCoords[type].snapped) {
      activeOrb = null;
      return;
    }
    
    e.preventDefault();
    
    const clientX = e.type === 'touchstart' ? e.touches[0].clientX : e.clientX;
    const clientY = e.type === 'touchstart' ? e.touches[0].clientY : e.clientY;
    
    const boardRect = board.getBoundingClientRect();
    const orbRect = activeOrb.getBoundingClientRect();
    
    // CACHE all DOM measurements at start of drag
    boardLeft = boardRect.left;
    boardTop = boardRect.top;
    boardWidth = boardRect.width;
    boardHeight = boardRect.height;
    
    targetX = boardWidth / 2;
    targetY = boardHeight / 2;
    
    offsetX = clientX - orbRect.left;
    offsetY = clientY - orbRect.top;
    
    document.addEventListener('mousemove', dragMove);
    document.addEventListener('mouseup', endDrag);
    document.addEventListener('touchmove', dragMove, { passive: false });
    document.addEventListener('touchend', endDrag);
  }
  
  function dragMove(e) {
    if (!activeOrb) return;
    e.preventDefault();
    
    const clientX = e.type === 'touchmove' ? e.touches[0].clientX : e.clientX;
    const clientY = e.type === 'touchmove' ? e.touches[0].clientY : e.clientY;
    
    // Calculate position relative to board using cached left/top offsets
    let x = clientX - boardLeft - offsetX;
    let y = clientY - boardTop - offsetY;
    
    x = Math.max(0, Math.min(boardWidth - 55, x));
    y = Math.max(0, Math.min(boardHeight - 55, y));
    
    const type = activeOrb.getAttribute('data-type');
    orbCoords[type].x = x;
    orbCoords[type].y = y;
    
    activeOrb.style.transform = `translate3d(${x}px, ${y}px, 0)`;
    
    checkSnapCollision(type);
  }
  
  function checkSnapCollision(type) {
    const orb = document.getElementById(`drag-${type}`);
    if (!orb) return;
    
    const orbCenterX = orbCoords[type].x + 27; // half of 55px width
    const orbCenterY = orbCoords[type].y + 27;
    
    // Distance using cached target X & Y (no DOM reads!)
    const dist = Math.hypot(orbCenterX - targetX, orbCenterY - targetY);
    
    if (dist < 45) {
      orbCoords[type].snapped = true;
      orb.classList.add('snapped');
      orb.style.pointerEvents = 'none';
      
      const snapX = targetX - 27;
      const snapY = targetY - 27;
      
      orbCoords[type].x = snapX;
      orbCoords[type].y = snapY;
      
      orb.style.transition = 'transform 0.6s cubic-bezier(0.25, 1, 0.5, 1), opacity 0.6s ease';
      orb.style.transform = `translate3d(${snapX}px, ${snapY}px, 0) scale(0)`;
      orb.style.opacity = '0';
      
      const snappedCount = Object.values(orbCoords).filter(o => o.snapped).length;
      triggerAudioBeep(260 + snappedCount * 120);
      
      endDrag();
      checkAllSnapped();
    }
  }
  
  function checkAllSnapped() {
    const allSnapped = Object.values(orbCoords).every(o => o.snapped);
    if (allSnapped) {
      setTimeout(triggerSuccessFlash, 500);
    }
  }
  
  function triggerSuccessFlash() {
    triggerAudioBeep(520);
    setTimeout(() => triggerAudioBeep(650), 120);
    setTimeout(() => triggerAudioBeep(780), 240);
    
    const targetGlow = document.querySelector('.mixer-dropzone');
    if (targetGlow) {
      targetGlow.style.boxShadow = '0 0 50px #fff, 0 0 100px var(--accent-color)';
      targetGlow.style.background = 'rgba(255,255,255,0.9)';
    }
    
    setTimeout(() => {
      journalUnlocked = true;
      const lockScreen = document.getElementById('journal-lock-container');
      if (lockScreen) {
        lockScreen.style.transition = 'opacity 0.8s ease';
        lockScreen.style.opacity = '0';
        
        setTimeout(() => {
          lockScreen.classList.remove('active');
          lockScreen.style.opacity = '';
          lockScreen.style.transition = '';
          
          const viewer = document.getElementById('document-viewer');
          if (viewer) {
            viewer.style.display = 'block';
            viewer.style.opacity = '0';
            viewer.style.transform = 'translateY(10px)';
            
            // Trigger reflow
            void viewer.offsetWidth;
            
            viewer.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
            viewer.style.opacity = '1';
            viewer.style.transform = 'translateY(0)';
            
            loadDocumentText('journal');
            
            setTimeout(() => {
                viewer.style.transition = '';
                viewer.style.transform = '';
            }, 800);
          }
        }, 800);
      }
    }, 1000);
  }
  
  function endDrag() {
    activeOrb = null;
    document.removeEventListener('mousemove', dragMove);
    document.removeEventListener('mouseup', endDrag);
    document.removeEventListener('touchmove', dragMove);
    document.removeEventListener('touchend', endDrag);
  }

  window.resetSynthesisOrbs = function() {
    const boardRect = board.getBoundingClientRect();
    
    orbCoords = {
      poetry: { x: 40, y: 30, snapped: false },
      lottery: { x: boardRect.width - 95, y: 30, snapped: false },
      art: { x: boardRect.width / 2 - 27, y: boardRect.height - 95, snapped: false }
    };
    
    orbs.forEach(orb => {
      orb.classList.remove('snapped');
      orb.style.pointerEvents = 'auto';
      const type = orb.getAttribute('data-type');
      orb.style.left = '0px';
      orb.style.top = '0px';
      orb.style.right = 'auto';
      orb.style.bottom = 'auto';
      
      // Instantly reset position and opacity without transitions
      orb.style.transition = 'none';
      orb.style.opacity = '1';
      orb.style.transform = `translate3d(${orbCoords[type].x}px, ${orbCoords[type].y}px, 0)`;
      
      // Force layout recalculation so the 'none' transition takes effect instantly
      void orb.offsetWidth;
      orb.style.transition = 'box-shadow var(--transition-fast)';
    });
    
    const targetGlow = document.querySelector('.mixer-dropzone');
    if (targetGlow) {
      targetGlow.style.boxShadow = 'none';
      targetGlow.style.background = 'none';
    }
  };
}

function resetSynthesisMixer() {
  if (window.resetSynthesisOrbs) window.resetSynthesisOrbs();
}

// -------------------------------------------------------------------------
// 8. Interactive Lottery Black Box drawing game (Brought directly from the story)
// -------------------------------------------------------------------------
let lotteryPhase = 'household';
let lotterySlips = [];
const TOWN_FAMILIES = ['Adams', 'Graves', 'Summers', 'Hutchinson', 'Martin'];
const HUTCHINSON_FAMILY = ['Bill', 'Tessie', 'Bill Jr.', 'Nancy', 'Davey'];

function initializeLotteryRitual() {
  const box = document.getElementById('lottery-box-graphics'); // Fixed DOM ID mapping
  const btn = document.getElementById('lottery-action-btn');
  
  if (box) {
    box.addEventListener('click', () => {
      if (lotterySlips.length === 0) {
        triggerLotteryDrawingSequence();
      }
    });
  }
  
  if (btn) {
    btn.addEventListener('click', () => {
      resetLotteryRitual();
    });
  }
}

function triggerLotteryDrawingSequence() {
  const board = document.getElementById('lottery-slips-board');
  const status = document.getElementById('lottery-status-msg');
  const box = document.getElementById('lottery-box-graphics'); // Fixed DOM ID mapping
  if (!board || !status) return;
  
  triggerAudioBeep(120);
  if (box) {
    box.style.animation = 'spotFlash 0.3s ease-out';
    setTimeout(() => box.style.animation = 'none', 300);
  }
  
  board.innerHTML = '';
  lotterySlips = [];
  
  const pool = lotteryPhase === 'household' ? TOWN_FAMILIES : HUTCHINSON_FAMILY;
  const targetWinner = lotteryPhase === 'household' ? 'Hutchinson' : 'Tessie';
  
  pool.forEach(name => {
    const isWinner = name === targetWinner;
    const slip = document.createElement('div');
    slip.className = 'lottery-slip';
    slip.setAttribute('data-name', name);
    slip.textContent = name;
    
    slip.addEventListener('click', () => {
      revealLotterySlip(slip, isWinner);
    });
    
    board.appendChild(slip);
    lotterySlips.push(slip);
  });
  
  status.textContent = lotteryPhase === 'household'
    ? "Household slips drawn. Click on each family tag to unfold their folded papers..."
    : "Family slips drawn. Click on each family member to see who holds the black spot...";
  status.classList.add('active-phase');
}

function revealLotterySlip(slipElement, isWinner) {
  if (slipElement.classList.contains('revealed')) return;
  
  slipElement.classList.add('revealed');
  
  if (isWinner) {
    slipElement.classList.add('black-spot');
    triggerAudioBeep(180);
    
    const status = document.getElementById('lottery-status-msg');
    const btn = document.getElementById('lottery-action-btn');
    
    if (lotteryPhase === 'household') {
      status.innerHTML = "<strong>Bill Hutchinson drew the spot!</strong> Proceed to drawing slips for individual members of the Hutchinson family.";
      btn.textContent = "Proceed to Family Drawing";
      btn.style.display = 'block';
      
      document.body.style.boxShadow = 'inset 0 0 60px rgba(229, 62, 62, 0.3)';
    } else {
      status.innerHTML = "<strong>Tessie Hutchinson drew the spot.</strong><br><em>'It isn't fair, it isn't right!'</em> The villagers close in with stones.";
      btn.textContent = "Reset Drawing";
      btn.style.display = 'block';
      
      document.body.style.boxShadow = 'inset 0 0 100px rgba(229, 62, 62, 0.6)';
      triggerHeartbeatPulses(true);
    }
  } else {
    triggerAudioBeep(440);
    slipElement.textContent = "Blank";
    slipElement.style.opacity = '0.5';
  }
}

function resetLotteryRitual() {
  const btn = document.getElementById('lottery-action-btn');
  document.body.style.boxShadow = 'none';
  
  if (lotteryPhase === 'household') {
    lotteryPhase = 'family';
    triggerLotteryDrawingSequence();
    if (btn) btn.style.display = 'none';
  } else {
    lotteryPhase = 'household';
    resetLotteryRitualGame();
  }
}

function resetLotteryRitualGame() {
  const board = document.getElementById('lottery-slips-board');
  const status = document.getElementById('lottery-status-msg');
  const btn = document.getElementById('lottery-action-btn');
  
  if (board) board.innerHTML = '';
  if (status) {
    status.textContent = "Click the black box to draw slips for the town's families.";
    status.classList.remove('active-phase');
  }
  if (btn) btn.style.display = 'none';
  lotterySlips = [];
  document.body.style.boxShadow = 'none';
}

// 9. Soundscape Music Synthesizer Engine (LPF Filters added to strip high buzzy frequencies)
let audioCtx = null;
let masterGain = null;
let humOsc = null;
let humGain = null;
let windNoiseNode = null;
let windFilter = null;
let windGain = null;
let chimeInterval = null;
let heartbeatInterval = null;
let synthGain = null;
let padOscs = [];
let audioEngineStarted = false;
let audioMuted = false; // Unmuted by default (Sound is on by default)

function initializeAudioEngine() {
  const btn = document.getElementById('sound-toggle');
  if (!btn) return;
  
  // 1. Synchronize button state with the default (unmuted)
  if (!audioMuted) {
    btn.classList.add('playing');
  } else {
    btn.classList.remove('playing');
    btn.innerHTML = `<i class="fa-solid fa-volume-xmark"></i>`;
  }
  
  // 2. Global click listener to start/resume audio on first interaction
  const initAudio = () => {
    if (audioMuted) return; // Do not resume if user explicitly muted
    
    if (!audioEngineStarted) {
      startSoothingSoundscapes();
      audioEngineStarted = true;
      btn.classList.add('playing');
    } else if (audioCtx && audioCtx.state === 'suspended') {
      audioCtx.resume();
      btn.classList.add('playing');
    }
  };
  
  document.addEventListener('click', initAudio, { once: false });
  document.addEventListener('mousedown', initAudio, { once: false });
  document.addEventListener('touchstart', initAudio, { passive: true, once: false });

  // 3. Sound toggle button click handler
  btn.addEventListener('click', (e) => {
    e.stopPropagation(); // Prevent document click handler from firing
    
    if (audioMuted) {
      // Unmute
      audioMuted = false;
      btn.classList.add('playing');
      
      if (!audioEngineStarted) {
        startSoothingSoundscapes();
        audioEngineStarted = true;
      } else if (audioCtx && audioCtx.state === 'suspended') {
        audioCtx.resume();
      }
    } else {
      // Mute
      audioMuted = true;
      btn.classList.remove('playing');
      btn.innerHTML = `<i class="fa-solid fa-volume-xmark"></i>`;
      
      if (audioCtx && audioCtx.state === 'running') {
        audioCtx.suspend();
      }
    }
  });
}

function startSoothingSoundscapes() {
  audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  
  // 1. Master Gain Node (Soft background level)
  masterGain = audioCtx.createGain();
  masterGain.gain.setValueAtTime(0.18, audioCtx.currentTime);
  masterGain.connect(audioCtx.destination);
  
  // 2. Soothing Home G-Major 7th Chord Pad
  synthGain = audioCtx.createGain();
  synthGain.gain.setValueAtTime(0.2, audioCtx.currentTime);
  synthGain.connect(masterGain);
  
  const chordFreqs = [98.00, 146.83, 246.94, 369.99]; // G2, D3, B3, F#4
  const lpf = audioCtx.createBiquadFilter();
  lpf.type = 'lowpass';
  lpf.frequency.setValueAtTime(140, audioCtx.currentTime); // High LPF cut to remove buzzy harmonics
  
  chordFreqs.forEach(freq => {
    const osc = audioCtx.createOscillator();
    osc.type = freq < 150 ? 'triangle' : 'sine';
    osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
    osc.connect(lpf);
    osc.start();
    padOscs.push(osc);
  });
  lpf.connect(synthGain);
  
  // 3. Sweep Gain LFO
  swellPadChordGain();
  
  // 4. Poetry wind flutter breeze (noise filter)
  const bufferSize = audioCtx.sampleRate * 2;
  const noiseBuffer = audioCtx.createBuffer(1, bufferSize, audioCtx.sampleRate);
  const data = noiseBuffer.getChannelData(0);
  for (let i = 0; i < bufferSize; i++) {
    data[i] = Math.random() * 2 - 1;
  }
  
  windNoiseNode = audioCtx.createBufferSource();
  windNoiseNode.buffer = noiseBuffer;
  windNoiseNode.loop = true;
  
  windFilter = audioCtx.createBiquadFilter();
  windFilter.type = 'bandpass';
  windFilter.Q.setValueAtTime(4.0, audioCtx.currentTime);
  windFilter.frequency.setValueAtTime(450, audioCtx.currentTime);
  
  windGain = audioCtx.createGain();
  windGain.gain.setValueAtTime(0.0, audioCtx.currentTime);
  
  windNoiseNode.connect(windFilter);
  windFilter.connect(windGain);
  windGain.connect(masterGain);
  windNoiseNode.start();
  
  sweepWindFilter();
  
  // 5. Chimes generator loop
  startChimesGenerator();
  
  // 6. Suspense heartbeat thumper
  startHeartbeatDrum();
  
  updateSoundscapeParameters(currentDocId || 'default');
}

function swellPadChordGain() {
  if (!audioCtx || !synthGain) return;
  const now = audioCtx.currentTime;
  const theme = document.body.getAttribute('data-theme');
  
  let targetVal = 0.22;
  if (theme === 'lottery') targetVal = 0.04;
  if (theme === 'art') targetVal = 0.38;
  
  synthGain.gain.setValueAtTime(synthGain.gain.value, now);
  synthGain.gain.linearRampToValueAtTime(targetVal, now + 3);
  synthGain.gain.linearRampToValueAtTime(targetVal * 0.4, now + 6);
  
  setTimeout(swellPadChordGain, 6000);
}

function sweepWindFilter() {
  if (!windFilter || !audioCtx) return;
  const now = audioCtx.currentTime;
  const target = 300 + Math.random() * 400; // soft sweeping LPF range
  windFilter.frequency.exponentialRampToValueAtTime(target, now + 4 + Math.random() * 3);
  setTimeout(sweepWindFilter, 7000);
}

function startChimesGenerator() {
  chimeInterval = setInterval(() => {
    if (!audioCtx || audioCtx.state !== 'running') return;
    const theme = document.body.getAttribute('data-theme');
    if (theme !== 'default' && theme !== 'poetry' && theme !== 'art') return;
    
    if (Math.random() > 0.4) return;
    
    const pentatonic = [783.99, 880.00, 1046.50, 1174.66, 1318.51];
    const freq = pentatonic[Math.floor(Math.random() * pentatonic.length)];
    playSingleSoothingChime(freq);
  }, 2200);
}

function playSingleSoothingChime(freq) {
  const osc = audioCtx.createOscillator();
  const gain = audioCtx.createGain();
  
  osc.type = 'sine';
  osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
  
  gain.gain.setValueAtTime(0, audioCtx.currentTime);
  gain.gain.linearRampToValueAtTime(0.03, audioCtx.currentTime + 0.2); // Soft attack curve
  gain.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + 3.8); // Long smooth release decay
  
  // Lowpass filter chimes to make them extremely mellow and comfortable
  const chimeLpf = audioCtx.createBiquadFilter();
  chimeLpf.type = 'lowpass';
  chimeLpf.frequency.setValueAtTime(750, audioCtx.currentTime);
  
  osc.connect(gain);
  gain.connect(chimeLpf);
  chimeLpf.connect(masterGain);
  
  osc.start();
  osc.stop(audioCtx.currentTime + 3.9);
}

function startHeartbeatDrum() {
  heartbeatInterval = setInterval(() => {
    if (!audioCtx || audioCtx.state !== 'running') return;
    const theme = document.body.getAttribute('data-theme');
    if (theme !== 'lottery') return;
    
    triggerHeartbeatPulses(false);
  }, 3000);
}

function triggerHeartbeatPulses(panicMode) {
  if (!audioCtx) return;
  
  const now = audioCtx.currentTime;
  const spacing = panicMode ? 0.22 : 0.32;
  const gainVal = panicMode ? 0.35 : 0.2;
  
  playSingleThump(now, gainVal);
  playSingleThump(now + spacing, gainVal * 0.7);
}

function playSingleThump(time, gainVolume) {
  const osc = audioCtx.createOscillator();
  const gain = audioCtx.createGain();
  
  osc.type = 'sine';
  osc.frequency.setValueAtTime(45, time); // Extremely low sub-bass pitch
  osc.frequency.exponentialRampToValueAtTime(1, time + 0.14);
  
  gain.gain.setValueAtTime(0, time);
  gain.gain.linearRampToValueAtTime(gainVolume, time + 0.02);
  gain.gain.exponentialRampToValueAtTime(0.0001, time + 0.16);
  
  osc.connect(gain);
  
  // Cut any harmonic click sounds
  const filter = audioCtx.createBiquadFilter();
  filter.type = 'lowpass';
  filter.frequency.setValueAtTime(65, time); // Strict sub-bass filter
  
  gain.connect(filter);
  filter.connect(masterGain);
  
  osc.start(time);
  osc.stop(time + 0.2);
}

function updateSoundscapeParameters(docId) {
  if (!audioCtx || !masterGain) return;
  const now = audioCtx.currentTime;
  
  switch(docId) {
    case 'poetry':
      windGain.gain.linearRampToValueAtTime(0.12, now + 1.5);
      break;
    case 'lottery':
      windGain.gain.linearRampToValueAtTime(0.02, now + 1.5);
      break;
    case 'art':
      windGain.gain.linearRampToValueAtTime(0.01, now + 1.5);
      break;
    case 'journal':
      windGain.gain.linearRampToValueAtTime(0.05, now + 1.5);
      break;
    default:
      windGain.gain.linearRampToValueAtTime(0.0, now + 1.5);
      break;
  }
}

function triggerAudioBeep(freq) {
  if (!audioCtx || audioCtx.state !== 'running') return;
  
  const osc = audioCtx.createOscillator();
  const gain = audioCtx.createGain();
  
  osc.type = 'sine';
  osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
  
  gain.gain.setValueAtTime(0, audioCtx.currentTime);
  gain.gain.linearRampToValueAtTime(0.04, audioCtx.currentTime + 0.06);
  gain.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + 0.4);
  
  const beepLpf = audioCtx.createBiquadFilter();
  beepLpf.type = 'lowpass';
  beepLpf.frequency.setValueAtTime(500, audioCtx.currentTime); // Muffle beeps for comfort
  
  osc.connect(gain);
  gain.connect(beepLpf);
  beepLpf.connect(masterGain);
  
  osc.start();
  osc.stop(audioCtx.currentTime + 0.5);
}

// 10. Lightweight Fallback Markdown parser
function parseMarkdown(md) {
  if (window.marked && window.marked.parse) {
    return window.marked.parse(md);
  }
  
  const lines = md.split('\n');
  let html = '';
  let inList = false;
  let inQuote = false;
  
  for (let line of lines) {
    let trimmed = line.trim();
    if (!trimmed) {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      continue;
    }
    if (trimmed === '---') {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      html += '<hr />\n';
      continue;
    }
    if (trimmed.startsWith('# ')) {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      html += `<h1>${trimmed.slice(2)}</h1>\n`;
      continue;
    }
    if (trimmed.startsWith('## ')) {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      html += `<h2>${trimmed.slice(3)}</h2>\n`;
      continue;
    }
    if (trimmed.startsWith('### ')) {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      html += `<h3>${trimmed.slice(4)}</h3>\n`;
      continue;
    }
    if (trimmed.startsWith('> ')) {
      if (inList) { html += '</ul>\n'; inList = false; }
      if (!inQuote) { html += '<blockquote>\n'; inQuote = true; }
      let quoteContent = trimmed.slice(2);
      quoteContent = parseInlineMarkdownElements(quoteContent);
      html += `<p>${quoteContent}</p>\n`;
      continue;
    }
    if (trimmed.startsWith('- ') || trimmed.startsWith('* ')) {
      if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
      if (!inList) { html += '<ul>\n'; inList = true; }
      let itemContent = trimmed.slice(2);
      itemContent = parseInlineMarkdownElements(itemContent);
      html += `<li>${itemContent}</li>\n`;
      continue;
    }
    if (inList) { html += '</ul>\n'; inList = false; }
    if (inQuote) { html += '</blockquote>\n'; inQuote = false; }
    
    let pContent = parseInlineMarkdownElements(line);
    html += `<p>${pContent}</p>\n`;
  }
  if (inList) html += '</ul>\n';
  if (inQuote) html += '</blockquote>\n';
  return html;
}

function parseInlineMarkdownElements(text) {
  let output = text;
  output = output.replace(/</g, '&lt;').replace(/>/g, '&gt;');
  output = output.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
  output = output.replace(/__(.*?)__/g, '<strong>$1</strong>');
  output = output.replace(/\*\((.*?)\)\*/g, '<em>$1</em>');
  output = output.replace(/\*(.*?)\*/g, '<em>$1</em>');
  output = output.replace(/_(.*?)_/g, '<em>$1</em>');
  output = output.replace(/!\[(.*?)\]\((.*?)\)/g, '<img src="$2" alt="$1" />');
  output = output.replace(/\[(.*?)\]\((.*?)\)/g, '<a href="$2" target="_blank">$1</a>');
  output = output.replace(/`(.*?)`/g, '<code>$1</code>');
  return output;
}

function updateFooterNavigation(docId) {
  const btnPrev = document.getElementById('btn-prev-doc');
  const btnNext = document.getElementById('btn-next-doc');
  if (!btnPrev || !btnNext) return;
  
  const currentIndex = DOC_SEQUENCE.indexOf(docId);
  if (currentIndex > 0) {
    btnPrev.style.visibility = 'visible';
    const prevId = DOC_SEQUENCE[currentIndex - 1];
    btnPrev.querySelector('.title').textContent = DOC_TITLES[prevId];
  } else {
    btnPrev.style.visibility = 'hidden';
  }
  
  if (currentIndex < DOC_SEQUENCE.length - 1) {
    btnNext.style.visibility = 'visible';
    const nextId = DOC_SEQUENCE[currentIndex + 1];
    btnNext.querySelector('.title').textContent = DOC_TITLES[nextId];
  } else {
    btnNext.style.visibility = 'hidden';
  }
}
